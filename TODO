DONE:
- std.process and paths WORKS with unicode characters

TODO:
- implement all arguments from readme
- add path normalizing to all config paths to make them absolute (slash()?)
- start compiling with std.process
- add argument to create a cmake or meson project for publishing

REM:
- to push `git add .` `git commit -m <commit name>` `git push origin main`