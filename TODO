TODO:
- Fix pipes and execution
- Make find_compiler find the compiler compiling buildit
- Incremental build (function for checking if file needs to be rebuilt)
- function to build all .c or .cpp files from folder (incrementally) (auto mode) + grep all .c or .cpp files for headers in
src folder and check if they need to be rebuilt
- Multithreading
- Progressbar
- rebuild yourself
- git helper
- templater
- fix all todos
- try to break (e.g. set all arguments to NULL, 0 or empty array or string)

- REWRITE IN C not C++
- dynamic arrays like nob but generic with e.g. DA(int) -> int* (signyfing that its a dynamic array not just a pointer)
- unordered DA e.g. UDA(int)
- linked list e.g. LL(int)

- Add some examples in comments

- Test on Linux, Windows, MacOS, BSD
