TODO:
- REWRITE in java (jpackage) or go or python (it has multithreading) or smth
- implement whole config from readme
- add subconfigs for platforms (windows, posix, linux, else)
- implement all arguments from readme
- add path normalizing to all config paths to make them absolute
- start compiling
- add $() commands in all config variables
- add argument to create a cmake or meson project for publishing
- multithreading
- progressbar

REM:
- to push `git add .` `git commit -m <commit name>` `git push origin main`